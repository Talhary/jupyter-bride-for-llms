version: "3.8"

services:
  jupyter-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jupyter-bridge
    restart: unless-stopped
    command: ["/app/jupyter-bridge", "--http"]
    ports:
      - "8080:8080"
    env_file:
      - .env
