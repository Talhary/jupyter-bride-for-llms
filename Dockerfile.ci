# Minimal production stage (~6.8MB)
FROM alpine:3.20

RUN apk add --no-cache ca-certificates tzdata

WORKDIR /app

# Copy pre-compiled binary from GitHub Actions runner
COPY jupyter-bridge /app/jupyter-bridge

EXPOSE 8080

USER 1000:1000

ENTRYPOINT ["/app/jupyter-bridge"]
